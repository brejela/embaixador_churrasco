<div class="content">
    <div class="row col-md-8 offset-md-2 content-inicio">
        <% for (var index = 0; index < cont.length; index += 1){ %>
            <h1>
                <%= cont[index].titulo %>
            </h1>
            <p class="text-justified">
                <%= cont[index].texto %>
            </p>
            <% } %>
    </div>
</div>